.DS_Store
.claude/memory/

# scratch / dead weight — never push
.playwright-mcp/
_to_delete/
_portfolio-git-broken-backup/

# local preview drafts — keep on disk, never publish
case-studies/_preview-*

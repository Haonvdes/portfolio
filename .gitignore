**/.DS_Store

# notes, research, specs and retired files — on disk only, never pushed
_private/

# Claude / editor / tooling — local only
CLAUDE.md
.claude/
.vscode/
*.code-workspace
node_modules/
.env
*.map

# scratch / dead weight — never push
.playwright-mcp/
_to_delete/
_portfolio-git-broken-backup/

# local preview drafts — keep on disk, never publish
case-studies/_preview-*

# Project Manager resume — private, sent as a PDF attachment, never deployed
Stephano-Ng-Resume-PJM.html
Stephano-Ng-Resume-PJM.pdf
